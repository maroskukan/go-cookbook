# Log Analyst

## Usage Examples

```bash
go run . -help
Usage of /tmp/go-build985561309/b001/exe/go-world:
  -level string
        Log level to search for. Options are DEBUG, INFO, ERROR, and CRITICAL (default "ERROR")
  -path string
        The path to the log that should be analyzed (default "myapp.log")
go run . -help
Usage of /tmp/go-build985561309/b001/exe/go-world:
  -level string
        Log level to search for. Options are DEBUG, INFO, ERROR, and CRITICAL (default "ERROR")
  -path string
        The path to the log that should be analyzed (default "myapp.log")
```

